# Minimum versions — installs on Python 3.10–3.14.
# (Exact pins like Pillow==11.0.0 have no prebuilt wheels on Python 3.14.)
fastapi>=0.115
uvicorn>=0.30
python-multipart>=0.0.12
ultralytics>=8.3.0
Pillow>=11.0.0
